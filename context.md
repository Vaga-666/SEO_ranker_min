# Контекст реализации

## Шаг 1 (2026-04-23)
- Прочитан и разобран `README.md`, зафиксированы ограничения MVP.
- Создан базовый каркас проекта на FastAPI + SQLAlchemy + Jinja2.
- Добавлены зависимости в `requirements.txt` и пример переменных окружения в `.env.example`.
- Реализованы SQLAlchemy-модели для таблиц:
  - `analysis_runs`
  - `serp_results`
  - `page_snapshots`
  - `page_features`
  - `page_scores`
  - `recommendations`
- Реализована стартовая веб-часть:
  - `GET /` с формой (поисковая фраза + целевой URL)
  - `POST /analyze` с созданием запуска анализа в БД
  - `GET /runs/{run_id}` для просмотра состояния запуска
- Добавлен stub-конвейер `run_analysis_stub`, который переводит запуск в статус `queued`.

## Что дальше (план следующего шага)
- Подключить реальный сбор top-10 SERP через Playwright (Yandex, desktop).
- Сохранять HTML и screenshot выдачи.
- Записывать результаты в `serp_results`.

## Шаг 2 (2026-04-23)
- Добавлен сервис `app/services/serp_yandex.py` для сбора desktop SERP Yandex через Playwright.
- Реализовано сохранение артефактов выдачи в `artifacts/runs/<run_id>/`:
  - `serp.html`
  - `serp.png`
- Пайплайн анализа обновлен:
  - статус запуска: `running` -> `serp_collected` / `no_results` / `failed`
  - запись top-10 результатов в таблицу `serp_results`
  - аккуратная обработка ошибок с фиксацией `error_message` в `analysis_runs`
- Страница запуска (`/runs/{run_id}`) теперь показывает таблицу собранного top-10.

## Что дальше (план следующего шага)
- Скачивание страниц конкурентов и целевого URL.
- Извлечение базовых признаков страницы (title, description, h1/h2/h3, текст и др.).
- Сохранение `page_snapshots` и `page_features`.

## Шаг 3 (2026-04-23)
- Добавлен сервис `app/services/page_fetch.py`:
  - загрузка HTML страницы через `httpx`
  - сохранение сырого HTML в `artifacts/runs/<run_id>/pages/`
  - возврат статуса загрузки (`ok`, `http_error`, `fetch_error`) и HTTP-кода
- Добавлен сервис `app/services/feature_extractor.py`:
  - извлечение `title`, `meta description`, `h1`, `h2/h3`, `main_text`
  - вычисление признаков: FAQ, таблицы, списки, CTA, формы, цены, отзывы, контакты, schema.org
  - подсчет внутренних и внешних ссылок
- Обновлен пайплайн `app/services/pipeline.py`:
  - после SERP автоматически обрабатываются страницы top-10
  - отдельно обрабатывается целевой URL, если его нет в top-10
  - сохраняются `page_snapshots` и `page_features`
  - запуск получает статус `pages_processed` или `pages_processed_partial`
- Обновлен UI страницы запуска:
  - показ сводки по скачанным страницам
  - таблица статусов загрузки (`source_type`, URL, статус, HTTP)
  - отображение количества извлеченных наборов признаков

## Что дальше (план следующего шага)
- Реализовать базовый scoring engine с весами из конфига.
- Заполнять `page_scores` и строить внутренний рейтинг.
- Добавить блок сравнения целевой страницы со средним top-10.

## Шаг 4 (2026-04-23)
- Добавлен конфиг весов `app/scoring_config.py`:
  - `intent_fit = 0.25`
  - `structure_fit = 0.20`
  - `semantic_coverage = 0.25`
  - `commercial_fit = 0.15`
  - `trust_fit = 0.15`
- Добавлен scoring-сервис `app/services/scoring.py`:
  - расчет 5 слоев по базовым эвристикам
  - weighted sum и итоговый `total_score`
- Обновлен пайплайн:
  - после извлечения признаков автоматически заполняется `page_scores`
  - рассчитывается внутренний ранг `internal_rank`
  - статусы запуска: `scored`, `scored_partial`, `scoring_skipped`
- Обновлена страница результата:
  - таблица top-10 с колонками из ТЗ: реальная позиция, наша позиция, URL, title, total score
  - блок сравнения целевой страницы со средним top-10 по 5 метрикам + total

## Что дальше (план следующего шага)
- Добавить AI-блоки (SERP intent analyzer + page analyzer) через OpenAI-compatible API с JSON-ответом.
- Реализовать базовый gap-анализ и формирование рекомендаций по 3 приоритетам.

## Шаг 5 (2026-04-23)
- Добавлен AI-сервис `app/services/ai_analyzer.py`:
  - `serp_intent_analyzer` (SERP summary)
  - `page_analyzer` (оценка страницы и короткие рекомендации)
  - режим строгого JSON (`response_format=json_object`)
  - безопасный fallback: при ошибке/отсутствии ключа AI не ломает пайплайн
- Добавлен gap-сервис `app/services/gap_analysis.py`:
  - базовый gap-анализ target страницы против top-10
  - генерация рекомендаций с приоритетами `urgent`, `medium`, `later`
- Пайплайн расширен:
  - после базового scoring запускается AI-уточнение score (если доступно)
  - пересчитывается `internal_rank`
  - сохраняются рекомендации в таблицу `recommendations`
  - финальные статусы: `done_ai`, `done_ai_partial`, `done_no_ai`, `done_no_ai_partial`
- UI результата обновлен:
  - блок рекомендаций в 3 категориях (срочно/желательно/можно отложить)

## Что дальше (план следующего шага)
- Привести README к фактическому состоянию MVP и добавить короткий сценарий запуска.
- Добавить минимальный тестовый сценарий запуска (smoke test для пайплайна).

## Шаг 6 (2026-04-23)
- Добавлен `.gitignore` (виртуальное окружение, артефакты, SQLite, `.env`).
- Полностью обновлен `README.md`:
  - быстрый запуск
  - переменные окружения
  - список реализованного MVP
  - ограничения текущей версии
  - команда запуска smoke-теста
- Добавлен `scripts/smoke_test.py`:
  - локальная проверка связки `feature_extractor + scoring`
  - вывод тестовых score-метрик
- В `scripts/smoke_test.py` добавлен фикс `sys.path`, чтобы скрипт корректно видел пакет `app`.

## Что дальше (план следующего шага)
- Установить зависимости в окружении и прогнать end-to-end запуск (`uvicorn` + реальный анализ формы).
- Добавить отдельное хранение/показ SERP intent summary и gap-данных (помимо текстовых рекомендаций).

## Шаг 7 (2026-04-23)
- Проведен реальный технический прогон окружения:
  - создано виртуальное окружение `.venv` на `Python 3.11`
  - установлены зависимости из `requirements.txt`
  - установлен Chromium для Playwright (`playwright install chromium`)
- Проверки:
  - `python -m compileall app scripts` — успешно
  - `python scripts/smoke_test.py` — успешно
- Проведен e2e-прогон через `TestClient`:
  - `GET /` — `200`
  - `POST /analyze` запускает пайплайн
- Проверен запуск через `uvicorn`:
  - сервер стартует корректно
  - `GET /` отвечает `200`
- Выявлен и исправлен важный кейс:
  - Yandex может вернуть страницу антибота `Вы не робот?`
  - добавлена детекция капчи в `app/services/serp_yandex.py`
  - в пайплайне добавлен явный статус запуска `captcha_detected` и понятная ошибка
- Обновлен `README.md`:
  - указана рекомендация использовать Python 3.11
  - добавлено поведение MVP при капче Yandex

## Что дальше (план следующего шага)
- Запустить проверку через `uvicorn` в браузере и пройти ручной сценарий формы.
- Добавить отображение причины `captcha_detected` в UI как отдельного предупреждающего блока.

## Шаг 8 (2026-04-23)
- Добавлен UI-блок предупреждения для `captcha_detected` на странице запуска анализа.
- Добавлен ручной fallback SERP:
  - на главной странице появилось поле `manual_top_urls` (URL по одному в строке)
  - если поле заполнено, анализ идет по этим URL без запроса к Yandex
- Реализована новая ветка пайплайна `run_analysis_with_manual_serp`:
  - запись ручного списка в `serp_results`
  - дальнейшие этапы (`pages -> scoring -> ai/recommendations`) выполняются как обычно
- Улучшен интерфейс формы:
  - добавлен `textarea` для ручных URL и поясняющий текст
  - добавлены стили warning-карточки
- Проверки после изменений:
  - `compileall` — успешно
  - `smoke_test` — успешно
  - e2e ручного режима: статус `done_no_ai`, `serp_results` заполняется
  - e2e автоматического режима при капче: статус `captcha_detected` с понятным текстом ошибки

## Что дальше (план следующего шага)
- Добавить на страницу результата отдельный блок «Gap-анализ» со структурированным выводом (не только рекомендации).
- Улучшить эвристики парсинга SERP и добавить повторную попытку с альтернативными селекторами/стратегией ожидания.

## Шаг 9 (2026-04-23)
- Реализован отдельный структурированный блок `Gap-анализ` на странице результата.
- В `app/main.py` добавлен расчет `gap_report` на основе `page_features` и сравнительных метрик:
  - `missing_blocks` — какие блоки есть у top-страниц, но отсутствуют у target
  - `missing_entities` — недостающие сущности/подтемы из заголовков/мета
  - `commercial_gaps` — просадки коммерческих сигналов
  - `trust_gaps` — просадки trust-сигналов
- Добавлены вспомогательные функции:
  - безопасный разбор `h2_h3_json`
  - выделение токенов/сущностей
  - дедупликация списков gap-пунктов
- Обновлен `app/templates/run_detail.html`:
  - новый визуальный блок с 4 подразделами gap-анализа
  - fallback-сообщения, если явных разрывов не найдено
- Проверки:
  - `compileall app` — успешно
  - e2e ручного режима через `TestClient` — страница результата рендерится, блок gap присутствует

## Что дальше (план следующего шага)
- Сделать более устойчивый SERP-парсер (альтернативные селекторы + retries) для снижения `captcha/no_results`.
- Добавить сохранение и показ `serp_summary` (dominant_intent/page_type и ожидания) в отдельном блоке UI.

## Шаг 10 (2026-04-23)
- Реализовано сохранение SERP summary в артефакты запуска:
  - файл `artifacts/runs/<run_id>/serp_summary.json`
  - сохраняется источник summary (`ai` или `basic_fallback`) и структура данных
- В `app/main.py` добавлено чтение `serp_summary.json` и передача данных в шаблон.
- В `app/templates/run_detail.html` добавлен отдельный блок:
  - `SERP / Intent Summary`
  - поля `dominant_intent`, `dominant_page_type`
  - списки `required_blocks`, `common_entities`, `commercial_expectations`, `trust_expectations`
- Проверки:
  - `compileall app` — успешно
  - e2e ручного режима через `TestClient` — блок summary отображается
  - файл `serp_summary.json` создается и содержит ожидаемые ключи

## Что дальше (план следующего шага)
- Повысить устойчивость SERP-сбора (retries + альтернативные стратегии ожидания/селекторы).
- Добавить compact-статусы этапов пайплайна в UI (SERP -> pages -> scoring -> AI -> recommendations).

## Шаг 11 (2026-04-23)
- Проверено подключение `.env` в рантайме через `app/config.py`.
- Подтверждено, что `SettingsConfigDict(env_file=".env")` корректно подхватывает:
  - `OPENAI_API_KEY`
  - `OPENAI_BASE_URL`
  - `OPENAI_MODEL`
- Выполнена техпроверка загрузки без вывода секрета:
  - API key читается (`OPENAI_API_KEY_SET=True`)
  - `OPENAI_BASE_URL` и `OPENAI_MODEL` подставляются из `.env`

## Что дальше (план следующего шага)
- Сделать e2e-прогон с реальным AI-ответом и проверить переход в статус `done_ai`/`done_ai_partial`.

## Шаг 12 (2026-04-23)
- Улучшена обработка ошибок SERP в `app/services/pipeline.py`:
  - добавлен форматтер исключений `_format_exception`
  - даже если `str(exc)` пустой, в `error_message` теперь сохраняется тип ошибки и `repr`
  - детекция капчи теперь смотрит по нормализованному тексту ошибки
- Обновлена детекция капчи в `app/services/serp_yandex.py`:
  - добавлен дополнительный маркер `not a robot`
- Проверка:
  - компиляция сервисов проходит
  - новый прогон по запросу `обучающая онлайн платформа` отрабатывает без падения (статус `no_results`)

## Что дальше (план следующего шага)
- Добавить ретраи и альтернативные стратегии ожидания SERP-элементов, чтобы уменьшить количество `no_results`.

## Шаг 13 (2026-04-23)
- Добавлено файловое отладочное логирование по каждому запуску анализа.
- Новый модуль: `app/services/debug_io.py`:
  - `append_run_log(...)` — пишет события в `debug_log.jsonl`
  - `save_run_json(...)` — сохраняет агрегированные JSON-артефакты
- Пайплайн (`app/services/pipeline.py`) расширен логами этапов:
  - старт пайплайна, SERP, pages, scoring, AI, завершение
  - логирование ошибок с traceback
  - логирование сохранения `serp_summary`
- Добавлено сохранение итогового файла:
  - `artifacts/runs/<run_id>/debug_result_summary.json`
  - содержит `status`, `error_message`, и счетчики (`serp_results`, `page_snapshots`, `page_features`, `page_scores`, `recommendations`)
- Обновлен `README.md` с описанием новых отладочных артефактов.
- Проверка: новый запуск создает оба файла; `debug_log.jsonl` читается и содержит события.

## Что дальше (план следующего шага)
- Добавить UI-кнопку/ссылку «Показать debug лог» на странице run detail.

## Шаг 14 (2026-04-23)
- Диагностирована причина падения `SERP fetch error: NotImplementedError`:
  - ошибка происходила внутри async Playwright при запуске subprocess (`asyncio.create_subprocess_exec`).
- Исправление:
  - сервис `app/services/serp_yandex.py` переведен с async Playwright на sync Playwright.
  - это убрало зависимость от event loop с поддержкой subprocess для данного шага пайплайна.
- Проверка:
  - компиляция модулей успешна
  - новый запуск с запросом `обучающая онлайн платформа` не падает с `NotImplementedError` (статус `no_results`)

## Что дальше (план следующего шага)
- Повысить качество извлечения SERP (альтернативные селекторы + retries), чтобы уменьшить частоту `no_results`.

## Шаг 15 (2026-04-23)
- Улучшен SERP-сбор в `app/services/serp_yandex.py`:
  - добавлены `retries` (до 3 попыток)
  - добавлены альтернативные селекторы блоков выдачи
  - добавлен fallback-парсинг через `BeautifulSoup` по HTML
  - добавлены детекторы `captcha` и `no_results_page`
  - в результат SERP добавлены поля `attempts` и `note`
- Обновлен пайплайн (`app/services/pipeline.py`):
  - в debug-лог записываются `attempts` и `note`
  - при пустой выдаче теперь сохраняется информативная ошибка:
    - `SERP empty: <reason>` (например `no_results_page_detected`)
- Проверка:
  - компиляция модулей успешна
  - прогон с запросом `обучающая онлайн платформа` завершился `no_results`, но уже с понятной причиной в `error_message`
  - в `debug_log.jsonl` есть `attempts=3` и `note`

## Что дальше (план следующего шага)
- Добавить еще один fallback: если SERP пустой, но есть ручной список URL, предлагать автоматический переход в manual-mode из UI.

## Шаг 16 (2026-04-23)
- Разобран инцидент запуска `#15` с ошибкой:
  - `SERP fetch error: NotImplementedError: NotImplementedError()`
  - трассировка показала падение Playwright при старте subprocess на Windows loop policy.
- Исправление в `app/services/serp_yandex.py`:
  - добавлен `_ensure_windows_proactor_policy()` перед стартом Playwright
  - policy принудительно переключается на `WindowsProactorEventLoopPolicy` при необходимости
  - модуль переписан в чистом UTF-8; строки детекторов вынесены через unicode-escape, чтобы исключить поломку кодировки
- Проверка:
  - компиляция модуля успешна
  - новый запуск с тем же запросом больше не падает с `NotImplementedError`
  - результат теперь диагностируемый: `no_results` + `SERP empty: no_results_page_detected`

## Что дальше (план следующего шага)
- Сделать UI-подсказку при `no_results`: предложить сразу повторить запуск в manual-mode с полем top-10 URL.

## Шаг 17 (2026-04-23)
- Добавлен интерактивный сценарий обработки капчи Yandex.
- В `app/services/serp_yandex.py` добавлена функция:
  - `fetch_yandex_top10_interactive(...)`
  - запускает видимый браузер (`headless=False`)
  - ждет ручного прохождения капчи
  - после прохождения автоматически пытается собрать top-10
- В `app/services/pipeline.py` добавлен этап:
  - `run_analysis_step_serp_interactive(...)`
  - сохраняет результаты в `serp_results` и продолжает пайплайн как обычно
- В `app/main.py` добавлен endpoint:
  - `POST /runs/{run_id}/retry-captcha`
- В `app/templates/run_detail.html` в блоке `captcha_detected` добавлена кнопка:
  - «Открыть браузер и пройти капчу»
  - и подсказка по использованию
- Проверка:
  - маршруты приложения содержат `/runs/{run_id}/retry-captcha`
  - компиляция обновленных модулей проходит

## Что дальше (план следующего шага)
- Добавить таймер/индикатор ожидания для интерактивного captcha-retry в UI.

## Шаг 18 (2026-04-23)
- Добавлен фоновый запуск интерактивного `captcha-retry`:
  - endpoint `POST /runs/{run_id}/retry-captcha` больше не блокирует HTTP-ответ
  - запуск интерактивного Playwright вынесен в background thread
  - статус запуска сразу переключается в `running_interactive_serp`
- В `run_detail` добавлен индикатор выполнения:
  - карточка `Анализ выполняется`
  - таймер прошедшего времени в секундах
  - статус-подсказка по текущему этапу
  - автообновление страницы каждые 5 секунд
- Добавлены стили для карточки выполнения (`card-running`).

## Что дальше (план следующего шага)
- Добавить кнопку «Открыть debug лог» на странице запуска и вывод последних N строк лога прямо в UI.

## Шаг 19 (2026-04-23)
- Улучшено качество отчета после реального прогона:
  1. Исправлено отображение `total_score = 0.0` в top-таблице:
     - теперь `0.0` показывается числом, а не `-`.
  2. Очищен список `missing_entities` от шума:
     - фильтруются годы/числа и типовые мусорные токены.
  3. Улучшен fallback `SERP / Intent Summary` без AI:
     - вместо `unknown` теперь строится эвристическая сводка:
       - `dominant_intent`
       - `dominant_page_type`
       - `required_blocks`
       - `common_entities`
       - `commercial_expectations`
       - `trust_expectations`
- Проверка на тестовом запуске:
  - summary больше не `unknown`
  - шум типа `2026` не попадает в сущности
  - `0.0` не отображается как `-`

## Что дальше (план следующего шага)
- Добавить встроенный просмотр debug-лога на странице запуска (последние 50 строк).

## Шаг 20 (2026-04-23)
- Добавлен AI-агент «рекомендации для Codex»:
  - новый сервис `app/services/codex_advisor.py`
  - на вход получает контекст запуска (status/error/counts)
  - на выход отдает структурированный JSON:
    - `summary`
    - `priority_fixes`
    - `quick_wins`
    - `diagnostics`
- Интеграция в пайплайн:
  - при финальных статусах автоматически сохраняется артефакт
    - `artifacts/runs/<run_id>/codex_advice.json`
  - событие сохранения пишется в `debug_log.jsonl`
- Интеграция в UI:
  - на странице run detail добавлен блок `Рекомендации для Codex`
  - выводятся приоритетные фиксы, quick wins и диагностика
- Проверка:
  - `codex_advice.json` создается
  - блок на странице результата рендерится

## Что дальше (план следующего шага)
- Добавить кнопку копирования `priority_fixes` в буфер для быстрого переноса в новый task.

## Шаг 21 (2026-04-23)
- Исправлена причина статуса `done_no_ai` при «рабочем ключе»:
  - в `app/config.py` изменен приоритет источников настроек: `.env` теперь выше системных ENV-переменных;
  - добавлена очистка строковых значений (`strip`, удаление обрамляющих кавычек);
  - `env_file` переведен на абсолютный путь от корня проекта.
- В `app/services/ai_analyzer.py` AI-настройки теперь читаются через `get_settings()` на каждый вызов.
- В `app/services/pipeline.py` добавлен диагностический лог `ai.config` (base_url, model, mask ключа, key_is_set).
- Проверка показала, что ранее брался системный ключ (`sk-yeK...`), а не ключ из `.env`; после фикса берется `.env` (`sk-pro...`) и AI-запросы проходят успешно.

## Шаг 22 (2026-04-23)
- Добавлен прогресс-бар после нажатия кнопки «Запустить анализ» на главной странице.
- В `app/templates/index.html`:
  - форма получила JS-обработчик submit через `fetch`;
  - добавлен UI-блок прогресса (процент, полоска, текст этапа);
  - кнопка блокируется на время запроса и меняет текст на «Запускаем...»;
  - после успешного ответа выполняется переход на страницу результата (`response.url`).
- В `app/static/style.css`:
  - добавлены стили progress bar и состояния `button:disabled`.
- Поведение: пользователь видит мгновенную обратную связь о ходе запуска анализа, вместо «тишины» до редиректа.

## Шаг 23 (2026-04-23)
- Реализован реальный прогресс запуска анализа (по статусам пайплайна), вместо имитации.
- В `app/main.py`:
  - `POST /analyze` переведен на фоновый запуск через thread (`_run_analysis_background`), чтобы UI не блокировался;
  - добавлен `POST /analyze-start` для JS-клиента (возвращает `run_id`, `status_url`, `detail_url`);
  - добавлен `GET /runs/{run_id}/progress` с фактическими полями прогресса (`status`, `percent`, `message`, `is_done`);
  - добавлен маппинг `_status_progress(...)` по этапам пайплайна;
  - `run_detail` теперь считает `created/serp_collected*` как running-состояния.
- В `app/templates/index.html`:
  - submit теперь идет на `/analyze-start`;
  - после старта форма поллит `/runs/{id}/progress` и обновляет progress bar по реальным данным;
  - по `is_done=true` выполняется автоматический переход на страницу результата.
- Проверка через `TestClient`:
  - `/analyze-start` возвращает корректный JSON;
  - `/runs/{id}/progress` возвращает реальный статус и процент (например `running_manual_serp`, `15`).

## Шаг 24 (2026-04-23)
- Исправлена битая кодировка в статус-подсказках страницы запуска (`app/main.py`):
  - `running_interactive_serp`, `running`, `running_manual_serp`, `pages_processed`, `pages_processed_partial`, `scored`, `scored_partial` теперь отображаются корректно на русском.
- Исправлен текст `error_message` при повторе капчи (`retry_captcha`) на читаемый русский.
- Проверка: `python -m compileall app/main.py` — успешно.

## Шаг 25 (2026-04-23)
- Добавлен ручной запуск «AI-анализ + рекомендации для Codex» из страницы результата.
- В `app/main.py`:
  - подключен `run_analysis_step_ai_and_recommendations`;
  - добавлен endpoint `POST /runs/{run_id}/rerun-ai-codex`;
  - запуск выполняется в фоне через `_run_ai_codex_background(...)`;
  - перед ручным запуском статус переводится в `running_ai_codex_manual`;
  - в `running_statuses`, `status_hints` и `_status_progress(...)` добавлен новый статус.
- В `app/templates/run_detail.html`:
  - добавлена кнопка «Запустить AI-анализ и рекомендации для Codex вручную» в блоке Codex.
- Проверка:
  - endpoint возвращает redirect `303`;
  - после завершенного run ручной запуск переводит статус в `running_ai_codex_manual` (прогресс 88%).

## Шаг 26 (2026-04-24)
- Переработан блок `Рекомендации для Codex` под SEO-задачу (улучшение контента target-страницы), а не под инженерную диагностику пайплайна.
- `app/services/codex_advisor.py` переписан:
  - новый фокус промпта: анализ target vs top-10 + gap-данные;
  - расширенная схема ответа: `root_cause`, `content_outline`, `copy_blocks`, `codex_task_prompt`;
  - сохранена обратная совместимость по старым полям (`summary`, `priority_fixes`, `quick_wins`, `diagnostics`);
  - fallback теперь формирует практичный SEO-бриф и готовый task-prompt для Codex.
- `app/services/pipeline.py` расширен контекст для советника:
  - добавлены реальные данные сравнения: `target_vs_top`, `gap_report`, `top_competitors`, `serp_summary`, сгруппированные рекомендации;
  - добавлены служебные helper-функции для расчета avg/diff и gap-полей.
- `app/templates/run_detail.html` обновлен:
  - в блоке Codex теперь отображаются `Root cause`, `Content outline`, `Copy blocks`, `Промпт для Codex`.
- `app/static/style.css`:
  - добавлен стиль `pre` для читабельного вывода большого prompt.
- Проверка:
  - компиляция модулей успешна;
  - тестовая генерация нового `codex_advice` возвращает заполненные `root_cause`, `codex_task_prompt`, outline и actionable fixes.

## Шаг 27 (2026-04-24)
- Обновлен `.gitignore`:
  - добавлены правила для Python-кэша и build-артефактов (`__pycache__`, `*.py[cod]`, `.pytest_cache`, `.mypy_cache`, `.ruff_cache`, `dist`, `build`, `*.egg-info`);
  - добавлены правила для локальных env/секретов (`.env`, `.env.local`, `.env.*.local`);
  - сохранено игнорирование локальной БД и артефактов (`seo_ranker.db`, `artifacts/`);
  - добавлены правила для IDE-файлов (`.idea`, `.vscode`).
